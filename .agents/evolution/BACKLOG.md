# 待办（BACKLOG）

候选改进清单。状态机：`idea → evaluated → accepted → in-progress → done | rejected | rolled-back`。
每日运行负责添加、评估、推进条目；每轮至多落地 1–2 个 `accepted` 且低风险的条目。
评估四问：①服务高质量/高效率/高稳定性？②与仓库架构兼容（插件而非改 agent-loop、能力接缝完整、测试政策）？③安全性影响（受保护路径/不可信输入）？④工作量与风险是否匹配收益？

| # | 条目 | 状态 | 优先级 | 评估/理由 | 落地记录 |
| --- | --- | --- | --- | --- | --- |
| 001 | 初始化种子：全仓库 hygiene 门禁（knip/publint/workspace 约束）基线扫描 | accepted | P1 | ①高质量✓ ②兼容：`pnpm run hygiene` 已是仓库命令，仅做基线记录不改架构 ③无安全影响，不触受保护路径 ④工作量低、风险低。**首落候选**（工作区干净后第一个落地） | 2026-08-21 验证轮评估；2026-08-24 第六轮 accepted（落地顺序第一位）；**2026-08-27 第八轮基线实测（首次）**：`pnpm run hygiene` = 11 passed / 2 failed（运行前后 `git status --porcelain` 无新文件，未污染工作区；失败与在途文件无关，属真实仓库基线弱点）——①constraints：`packages/client/schema-form`、`packages/client/web-react` 空壳残留目录（仅 `lib/`+`node_modules`，无 package.json；git log 显示为 refactor(client) cf5e686408 合并后未清理的磁盘残留）→ 修复=`pnpm run clean`；②vendor rescope：`knip.json` 中 `packages/util/home` workspace 块已不存在（moved，rescope-vendor.ts L201-214 期望片段过时）+ `docs/cookbook/adding-a-vendored-package.zh.md` 实际链接为 `../rescope.zh.md` 而检查期望 `../rescope.md`（partial，L348-355 期望过时）→ 修复=更新 rescope-vendor.ts 两处期望片段。**修复清单已就绪，001 落地时执行（需完整模式）**；**CI 覆盖归因（收敛补充）**：`rescope-vendor:check` 不在 CI（仅本地 hygiene/check-all），constraints 失败为本地机器残留（CI 全新建树不受影响）→ 上游 CI 不因此红，但 codemod 与树失同步真实存在（下次 vendor 同步会咬），001 落地时一并修复；第八轮由每日 08:05 定时实例与本次会话并行各跑一轮，双方独立复现同一基线（交叉验证） |
| 002 | 初始化种子：审计 hand-rolled-where-dependency-exists 候选 | evaluated | P1 | ①高质量/代码卫生✓（仓库已有 dependencies-over-hand-rolling 政策 note）②兼容：审计只读产出报告，不直接改代码 ③无安全影响 ④工作量中（跨包扫描）、风险低 | 2026-08-21 验证轮评估 |
| 003 | 工具输出/检索内容「不可信数据」边界标记审计（对照 spotlighting/StruQ 结构化分隔，OWASP LLM01；The Attacker Moves Second 显示检测型防御可被绕过，架构性分隔才是主控） | evaluated | P1 | ①安全性✓（提示注入第一防线：数据与指令分离）②兼容：优先走插件扩展点，但可能触及 `core/`、`session/` 的模型可见输入组装（受保护路径）③本身是加固，但改动面贴近受保护路径 ④工作量中-高；改动模型可见输入需按测试政策补快照 | 2026-08-21 验证轮评估；落地需人工授权（见 STATE 待人工决策） |
| 004 | 系统提示「前缀冻结 + 易变尾部」审计（Anthropic 上下文工程：mutating system prompt mid-session 破坏前缀缓存 → 延迟/成本） | evaluated | P2 | ①高效率✓（前缀缓存命中）+高质量 ②兼容：system-prompt 组装在 `core/`、`session/`（受保护路径）③中性偏正 ④工作量中、风险中 | 2026-08-21 验证轮评估；落地需人工授权 |
| 005 | 日志/输出凭据脱敏审计（Frontier Model Forum：scrub credentials from output/logs） | evaluated | P1 | ①安全性✓ ②兼容：`credentials/`、`guard/`、`session/` 均属受保护路径 ③加固，但改动面全在受保护路径 ④工作量中、风险中 | 2026-08-21 验证轮评估；落地需人工授权 |
| 006 | web 外部输入（fetch/搜索）与工具输出进入模型输入前的指令边界强化（间接提示注入主要入口是 web 内容） | evaluated | P2 | ①安全性✓ ②兼容：`packages/web/` 不在受保护路径，但跨 core prompt 组装 ③加固方向 ④工作量中-高 | 2026-08-21 验证轮评估 |
| 007 | agent 循环小型 eval 基线集（Anthropic：eval 驱动工具/提示演进；回归护栏） | evaluated | P2 | ①高稳定性/高质量✓ ②兼容：`packages/support/` 或 `scripts/`，非受保护路径，符合测试政策 ③中性 ④工作量中（部分需 key 的 e2e 或快照） | 2026-08-21 验证轮评估 |
| 008 | 工具 ACI（agent-computer interface）描述质量审计（Anthropic Writing effective tools：工具描述/参数质量直接决定模型调用行为） | evaluated | P2 | ①高质量✓ ②兼容：跨包 JSDoc/工具描述，低风险 ③中性 ④工作量低-中 | 2026-08-21 验证轮评估 |
| 009 | MCP 工具描述信任边界审计（MCP spec 2025-11-25 + CSA Agentic MCP Security v1：工具描述被隐式信任→工具投毒根因；mcp-client tools.ts 将远端 `tool.description` 原样透传进模型提示，无信任标记/来源标注） | evaluated | P1 | ①安全性✓（与 003/006 同族但落点独立：外部 server 提供的描述=不可信数据）②兼容：`packages/mcp/` 非受保护路径；审计只读；若落地涉及模型可见输入需会话事件+快照（testing policy）③加固方向 ④工作量低-中，审计先行 | 2026-08-21 第二轮评估；**2026-08-24 第五轮只读审计深化（双证据链确认缺口）**：①本地代码失衡证据——同一文件 `tools.ts` 对返回值侧（`extractText`/`decodeImage`/`projectContent`，L197-200/337-341/494-496 明确标注 network trust boundary）防御严谨，唯独 `tool.description ?? ''`（L166）经 `createDefinition` 原样透传为模型可见 `ToolDefinition.description`（L257-258），无信任标记/来源标注/结构化分隔；②外部权威——OWASP MCP Top 10（MCP06 Intent Flow Subversion：工具描述被当作可信指令读取，防御=context isolation/instruction quarantine）、imti.co（description/output/annotations 应 allow-listed/screened/consented/flagged）、Christian Schneider「Securing MCP: defense-first」Layer 3（tool description 即注入向量）；③落点精确：`createDefinition` 描述组装处；落地形态建议=描述来源标注（如 server 名）+可选指令隔离前缀，需评审+按 testing policy 补快照；**2026-08-26 第七轮复验+落地形态细化**：新 checkout b150a551b8 上证据行未变（tools.ts L165-166 `tool.description ?? ''` → createDefinition L251 → L258 模型可见 `ToolDefinition.description`）；外部新权威——官方 MCP Client Best Practices（2026-07-28 spec：naive host 把每个 server 工具定义原样塞给模型是反模式，客户端侧策展为最佳实践）、官方 Tool Annotations blog（2026-03-16：annotations 为「hint」，客户端 MUST 默认不可信，安全保证放确定性控制）、DigitalApplied「19 server 审计 taxonomy」（tool description=server 编写、必填、直接展示给模型）→ 按官方 spec 语义把远端描述视为不可信 hint，最小改动=描述来源标注（server 名前缀）+可选指令隔离，评审后按 testing policy 补快照 |
| 010 | MCP 工具定义变更监控（rug-pull 防御，CSA Level 2：工具定义视为版本化不可变工件，跨会话/重连变更应告警；mcp-client HMR 热重连可静默改变工具定义） | evaluated | P2 | ①安全性/稳定性✓ ②兼容：mcp-client 连接/重连处对工具定义做指纹对比+变更事件 ③加固方向 ④工作量中 | 2026-08-21 第二轮评估；**2026-08-24 第五轮只读审计深化（缺口确认+落点明确）**：`connection.ts` L156-165 序列化所有 `syncTools` 调用（初始 sync + notification re-sync），重连成功即重新 sync（L304）；`syncTools` swap 阶段（tools.ts L176-192）dispose 旧 generation 并注册新定义，**对新旧 generation 无指纹/差异比较**——重连或 `notifications/tools/list_changed` 可静默替换工具定义（含描述）。外部权威：Christian Schneider「Clients should re-prompt users when descriptions change materially, or at minimum log such changes for security review」。落点：swap 阶段加定义指纹对比（name+description+inputSchema 哈希）+ 变更日志/事件；现有 `reconnect.spec.ts` 已覆盖重连路径，落地有测试基础。**落地形态注意**：若按「变更时 re-prompt 用户」实现会触及 `interaction/`（受保护路径）→ 需人工决策（见 STATE 待人工决策）；**2026-08-26 第七轮复验**：connection.ts L156-166 `enqueueSync` 串行化所有 syncTools（初始+notification re-sync）、L304 重连成功即 re-sync，swap 阶段无指纹对比（grep fingerprint/hash 无命中）；官方 Tool Annotations「实际安全保证放确定性控制」→ 最小安全子集（指纹对比+变更日志事件）为 spec 合规基线，re-prompt 仅可选 UX（不触 interaction/ 的形态可行） |
| 011 | 启动上下文成本基线测量（arXiv 2603.05344：全量工具 schema 启动加载占 40% 上下文预算，懒加载降至 <5%；用 tokenMeter 测会话首请求 system prompt+tools 占比，产出可测基线进 STATE 进化杠杆） | evaluated | P2 | ①高效率✓（可测指标）②兼容：只读测量脚本/快照，非受保护路径 ③中性 ④工作量低-中 | 2026-08-21 第二轮评估 |
| 012 | 工具失败错误分类定向 nudge（permission denied / file not found / syntax error / rate limit → 分类恢复提示；arXiv 2603.05344） | evaluated | P2 | ①高效率/质量✓ ②兼容性待核查：可能落在 guard/agent-loop；若落点 guard 则受保护路径需授权 ③中性 ④工作量中 | 2026-08-21 第二轮评估；受保护路径待核查 |
| 013 | `.agents/skills/` 技能目录质量审计（Anthropic skill authoring best practices：描述/触发词精确性决定路由准确率与上下文成本，含糊触发词不可路由；示例完备性；捆绑脚本安全审计——社区教训：未文档化网络调用与脚本投毒） | evaluated | P2 | ①高质量/高效率✓（技能描述质量=路由准确率；每次加载仅 ~100 token/个，质量差则白烧上下文）②兼容：`scripts/verify-skill-invocation-metadata.ts` 已门禁元数据格式，本条目补齐内容质量维度；只读审计，不触受保护路径 ③安全✓（捆绑脚本审计呼应「禁止未审查依赖」护栏）④工作量低、风险低 | 2026-08-22 第三轮评估；只读报告可在研究模式产出；**2026-08-22 第四轮轻量核验**：技能目录 12 个技能、均有 SKILL.md description；唯一捆绑脚本 `record-browser-gif/scripts/encode_gif.py`（28K 技能内）仅用 argparse/json/math/subprocess/tempfile，无 requests/urllib/socket/urlopen 网络导入 → 脚本供应链面干净，内容质量维度（描述精确性/示例完备性）待产出报告；**2026-08-27 第八轮评估维度补强**（Claude Platform「Skill authoring best practices」2026 版 + 自改进技能模式共识）：description 必须同时含「做什么 + 何时用」（与既有维度一致）；自改进技能三要素=二进制 pass/fail evals（主观评分不可靠）+ memory 教训累积 + wrap-up 步骤分离——已由 014 与 STATE 护栏 6（评估器外置）覆盖，无需新条目 |
| 014 | 进化循环「MEMORY 应用闭环」核查（每轮核对上一轮 MEMORY 教训是否被实际应用；未应用则 RUNLOG 记录原因。对照 arXiv 2512.17102 + 社区 self-improving skill 模式：eval + memory 必须形成闭环，否则记忆成为死文档） | evaluated | P2 | ①高效率/稳定性✓（防止教训沉淀成摆设，直接提升每日循环质量）②兼容：进程级改进，仅改 `.agents/skills/dsh-self-evolve/SKILL.md` + 状态文件，无代码 ③中性 ④工作量极低 | 2026-08-22 第三轮评估；**2026-08-22 第四轮已应用**：MEMORY「漂移实测」「立项前核对结构」教训均在本轮执行（fetch 后实测 743、015/016 立项前 grep 现有实现），014 核查通过 |
| 015 | 工具瞬时失败 retry/backoff 插件（transient tool failure：网络抖动/进程崩溃导致工具调用失败时自动重试；对照 agent-loop 工具链 best practice「Retry and backoff—handling transient tool failures」；llm-retry 仅覆盖模型请求层） | evaluated | P2 | ①高效率/稳定性✓（工具瞬时失败是 agent-loop 常见噪声源）②兼容：`guard/timeout-policy/src/index.ts` L36 注释已预留「a retry/sandbox plugin (and replay) can route on it」接缝；需限定幂等/只读工具才可自动重试（非幂等工具重试=双重副作用）③落点 guard → **受保护路径，落地需人工授权**；且自动重试本身有副作用放大风险，需白名单+上限 ④工作量中、风险中 | 2026-08-22 第四轮评估；只读核验：`llm-retry`（模型请求重试）+ `core/session/repair.ts`（崩溃恢复）均非工具执行层重试，缺口真实存在；落地需人工授权（guard） |
| 016 | 会话级 token/成本预算断路器（per-session token/cost budget + circuit breaker：预算超限即熔断；对照 exemplar.dev agent-loop tooling「token and cost budgets with circuit breakers per session」） | evaluated | P2 | ①高效率/质量✓（失控循环的成本护栏，可测指标）②兼容：需核对 agent-loop 现有 maxTokens 语义（仅单请求输出上限）与会话事件流；若改 agent-loop 需同步 docs/architecture.md 与 SDK 表面 ③中性偏正 ④工作量中-高、风险中 | 2026-08-22 第四轮评估；只读核验：`agent-loop` maxTokens 仅单请求输出上限、无会话累计预算/熔断 → 缺口真实存在；落点若涉 guard/agent-loop 需授权，建议先出设计再立项；**2026-08-24 第六轮外部印证**（dev.to budgets 五维 + Medium 200+ 步失控案例 + arXiv「随会话增长资源必须有上限」），与 018（步骤预算）同族不同维度 |
| 017 | 进化循环「弱点评测先行」协议修订（选题前先读 RUNLOG/待人工决策，从门禁失败/自愈事件挖掘重复弱点优先研究；对照 Self-Harness propose-evaluate-accept 与 AHE 可观测性：每个失败定位到组件、每个编辑有证据） | done | P2 | ①高效率✓（研究选题从轮换制升级为弱点驱动，闭环证据）②兼容：进程级改进，仅改 `.agents/skills/dsh-self-evolve/SKILL.md`，无代码 ③中性 ④工作量极低 | 2026-08-22 第四轮落地：SKILL.md 研究节新增「弱点评测优先」步骤（commit 见 RUNLOG）；评估器与权限外置原则（Weng 2026-07-04）同步写入 STATE 护栏 |
| 018 | agent-loop 会话级步骤/迭代预算（runaway loop 防护：模型持续产出工具调用时无会话级上限——arXiv 2603.05344「Iteration limits prevent runaway agent loops」+「随会话增长资源必须有上限」、dev.to harness「budgets(step/time/token/cost) 耗尽即优雅停止」、Medium 生产案例「无硬预算 → loop 跑 200+ 步」） | evaluated | P2 | ①高稳定性/高效率✓（失控循环的成本与资源护栏，可测指标）②兼容性需决策：`packages/core/agent-loop/src/agent.ts` L199/L212/L263/L339 的 while 循环仅由模型产出终止，唯一上限 `maxTokens` 是单步输出上限（sticky）、`constants.ts` 仅 `DEFAULT_MAX_PARALLEL_TOOL_CALLS=10`（单步并行上限，已存在）——会话级步骤上限真空缺；`apps/cli` 亦无 turn 限制；**落点若改 agent-loop**（非受保护路径）需更新 docs/architecture.md + SDK 表面 + testing policy 快照；**落点若做 guard 插件**（事件计数熔断）则受保护路径需授权 → 落地形态需人工决策（与 016 同族：016=token/成本预算，018=步骤预算）③中性偏正 ④工作量中、风险中 | 2026-08-24 第六轮只读核实（缺口确认：行级证据 + 外部三源共识）；落地形态待人工决策；**2026-08-26 第七轮外部权威再+1**：Oracle「Agent Loop Decoded」显式停止条件清单——iteration cap（其引擎默认 10）、repetition detection（同 tool+args 连续 3 次，对应已存在的 repeat-tool-reminder [3,5,8] nudge）、wall-clock timeout、unrecoverable errors；硬迭代上限仍真空缺 |
| 019 | StackOne Defender 式提示注入检测模型（22MB CPU-only，~4ms，89% 准确率，工具结果进上下文前拦截） | rejected | — | ①安全性名义✓但实际不采纳：检测型防御（spotting）可被自适应攻击绕过（第一轮 The Attacker Moves Second 已确立：检测只是辅助层，架构性分隔才是主控）；②引入未审查第三方依赖+模型权重，违反「禁止据此安装未审查依赖」护栏；③与仓库已有架构级条目 003/006/009（数据/指令架构性分离）重叠 | 2026-08-24 第六轮评估拒绝（按既定原则直接拒绝，不重复完整评估） |
| 020 | harness 会话产物目录入 .gitignore（`.dsh-pilot/`、`.dsh-uploads/` 为 DSH 自身会话产物——截图/下载/上传缓存，持续污染 git status 并反复触发研究模式降级误判） | evaluated | P2 | ①高效率/高质量✓（git status 干净度；把「工具产物」与「用户在途工作」区分开）②兼容：根 `.gitignore` 追加两行，不影响任何包 ③无安全影响（目录仅会话产物，无代码）④工作量极低、风险极低 | 2026-08-26 第七轮评估（现状：`git check-ignore` 未覆盖，`.gitignore` 仅有 `.dsh-build/`）；落地需完整模式，属 001 之后的 quick win |
