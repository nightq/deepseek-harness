# 运行记录（RUNLOG）

每日追加；保留最近 90 天。条目格式：日期、研究主题、发现、BACKLOG 变更、落地内容、验证结果、PR/commit 链接、下一步建议。

| 日期 | 主题 | 发现 | 落地 | 验证 | 链接 | 下一步 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-08-21 | （初始化） | 基础设施建立：协议 SKILL + 状态文件 | 无代码改动 | — | — | 首轮验证运行 |
| 2026-08-21 | 安全加固（提示注入防御/工具沙箱化）+ agent 循环最佳实践（上下文工程/工具 ACI） | ①The Attacker Moves Second（arXiv 2510.09023）：自适应攻击可绕过已发表防御（>90% 成功率），检测型防御只是辅助层，架构性削减爆炸半径（Meta Rule of Two：无监督单次运行至多允许「处理不可信输入/访问私有数据/对外通信」三选二）才是主控；②OWASP LLM01/Agentic Top 10：提示注入/过度代理/系统提示泄漏；结构化提示分隔（StruQ/spotlighting）把数据与指令分离；③Frontier Model Forum：harness 层（监控/校验/日志/强制）是自然控制点，凭据需从输出/日志中脱敏；④Anthropic：系统提示前缀冻结+易变尾部（缓存友好）、eval 驱动工具演进、工具描述（ACI）质量直接决定模型行为 | BACKLOG：001/002 idea→evaluated（P1）；新增 003-008（003/005 P1 安全、004/006/007/008 P2），003-005 触碰受保护路径 | **无代码改动**（降级研究模式：工作区脏，34 文件 web bind-address 功能在途，含 `packages/host/webserver/*`、`packages/bundle/web-app/*`、`docs/*`、`apps/cli/*`；按协议「工作区脏时提交或推送」硬性禁止，fork/PR 发布路径验证顺延） | 无门禁（无代码改动）；发布路径前置检查（只读）：gh 账号 `nightq`、fork `nightq/deepseek-harness` 尚不存在、master @ 99f6f02fec | —（降级不推送，无 PR） | ①等工作区干净后先打通发布路径：`gh repo fork --remote=false` → `git push <fork-url> evolve/state` → `gh pr create`；②然后落地 001（hygiene 基线，低风险）；③安全类条目（003-005）待人工授权受保护路径 |
| 2026-08-21 | MCP 生态（工具信任边界/投毒/rug-pull）+ 性能（启动上下文成本/懒加载/doom-loop/分级压缩） | ①MCP spec 2025-11-25 + CSA Agentic MCP Security v1：工具描述被隐式信任→工具投毒/rug-pull 根因；建议工具定义版本化+哈希+变更监控（ETDI/Level 2）；本地 server 需同意机制+最小权限沙箱；CVE-2025-6514（mcp-remote 代理，43.7 万安装）展示代理供应链风险；②arXiv 2603.05344（Terminal coding agent）：全量工具 schema 启动加载占 40% 上下文预算、懒加载 <5%；错误分类定向 nudge；doom-loop 指纹（3 次重复）检测；分级压缩（>0.85 prune 旧工具输出 / >0.80 mask 旧观测为 ref）；③LangChain harness 工程：时间预算提醒、循环检测中间件；addyosmani：技能按需暴露、tool-call offloading 保 head/tail；④AgentCgroup（arXiv 2602.09345）：OS 执行占端到端延迟 56-74%，内存是并发瓶颈；⑤对照仓库现状：`packages/mcp/mcp-client` 存在且 tools.ts 原样透传远端 description（无信任标记）；guard `repeat-tool-reminder` 已实现阈值 [3,5,8] 重复调用提醒（doom-loop 已覆盖且更优）；compaction `compaction-tool-result-pruner` 已实现 head/middle/tail 剪枝+shadow-price（offload 已覆盖）；skill 按需加载已有 | BACKLOG：新增 009-012（009 P1 安全 / 010-012 P2）；无超 60 天 `idea` 条目需清理；001-008 不变 | **无代码改动**（研究模式：工作区 39 项在途，36 项属 web bind-address 在途功能 + 3 项进化目录本身；在途清单同上一轮并新增 `2026-08-20-explicit-web-interface-bind` note 3 文件） | 无门禁（无代码改动）；自愈检查：无已合并进化改动，无需回滚；上游漂移：本地落后 origin/master 536 提交（本地 99f6f02fec，origin dsh-v0.1.0-rc.8），研究模式不强行同步 | commit `a7c965bedf`（本地 master）→ fork `evolve/state`（`https://github.com/nightq/deepseek-harness/tree/evolve/state`）；**PR 创建失败**：GraphQL「nightq 无权 CreatePullRequest」+ REST pulls 端点 404（认证与匿名均 404，token 对上游仅 `pull:true`）→ 待人工建 PR | ①工作区干净后进入完整模式，先落地 001（hygiene 基线）；②009 MCP 描述信任审计（只读）优先于落地；③受保护路径条目（003-005、012 若涉 guard）待人工授权；④下轮研究建议 #4 技能自我进化 + #7 依赖与代码卫生 |
| 2026-08-22 | 技能自我进化（#4）+ 依赖与代码卫生（#7） | ①Anthropic skill authoring best practices：技能仅名称+描述常驻上下文（~100 token/个），正文按需加载；高质量技能=精确触发词+示例+无未文档化网络调用，含糊触发词（如「productivity tasks」）不可路由；②self-improving skill 模式（EvoSkill / Behind the Craft / arXiv 2512.17102）：evals.md（可自检 pass/fail）+ memory.md（教训累积）+ skill-editor（精简）构成闭环——本仓库 dsh-self-evolve 的 STATE/MEMORY/BACKLOG/RUNLOG 已是该模式实例，但缺「教训应用闭环」核查（→ 014）；③knip 是 dead-code 标准检测器（unused files/imports/exports、unlisted deps），成熟代码库 10-30% 死代码，CI 拦截最有效——仓库 `pnpm run hygiene` 已含 knip+publint+workspace 约束+NodeNext 检查，001 基线落地即覆盖，无新工具缺口；④技能供应链安全：社区技能捆绑脚本需审计（呼应护栏「禁止未审查依赖」） | BACKLOG：新增 013（技能目录质量审计 P2）、014（MEMORY 应用闭环 P2）；**009 现状只读核验完成**：读 `packages/mcp/mcp-client/src/tools.ts`，L166 `tool.description ?? ''` 与 `tool.inputSchema` 原样透传进 `ToolDefinition.description/parameters`（模型可见），无信任标记/来源标注；文件其余部分（extractText/decodeImage/projectContent）对网络信任边界防御严谨，唯独描述透传无标注→009 结论成立，落点=createDefinition 描述组装处；010 落点=syncTools 重新生成处 | **无代码改动**（研究模式：工作区 39 项在途 = 37 项 web bind-address 功能（含 3 个 `2026-08-20-explicit-web-interface-bind` note 文件）+ 2 项进化目录残留（第二轮遗留 SKILL.md/STATE.md 协议修订：--no-verify/凭据预检/PR 被拒处理，本轮随状态提交带走）） | 无门禁（无代码改动）；自愈检查：无已合并进化改动、无回滚指示→无需回滚；上游漂移：`git fetch origin` 后实测**落后 743 提交**（上轮 536；origin 到 dsh-v0.1.1-rc.2，本地 32a80e9841），研究模式不强行同步 | commit `5e6cc40f27` → fork `evolve/state`（`https://github.com/nightq/deepseek-harness/tree/evolve/state`）；PR 创建预期仍被拒（权限未变），被拒后不重试、记入待人工决策 | ①建议人工优先收尾 web bind-address 在途功能——漂移 743 且持续扩大，过期基线落地成本递增；②工作区干净后完整模式先落地 001（hygiene 基线）；③009 只读审计报告→人工评审后落地信任标记；④013 技能目录只读审计可在研究模式产出；⑤受保护路径条目（003-005、012）待人工授权 |
| 2026-08-22 | Cordis/Koishi 插件生态（#3）+ agent 循环最佳实践（#1）+ 自进化 harness 文献 | ①Cordis v4 机制（ctx.effect 可逆副作用/ready-dispose-fork 生命周期/服务依赖排序）与热重载（无重启不掉 agent 状态）本仓库已内化（vendor/cordis + self-modification + demo:cordis），无新缺口；②agent 循环四杠杆诊断（Context/Tools/Loop/Governance，damiangalarza.com）；工具瞬时失败 retry/backoff 是真实缺口（exemplar.dev 工具链共识）；「Stop Overengineering Your Agent Harness」印证「不为改而改」护栏；③Lilian Weng 2026-07-04 Harness Engineering for Self-Improvement + Self-Harness（arXiv 2606.09498）+ AHE（arXiv 2604.25850）：自进化循环会过拟合奖励信号→评估器与权限控制必须外置（held-out tests/trace audits/人工决策点）；weakness mining → bounded proposal → validation 与我们协议同构；AHE 可观测性=每次失败定位组件、每个编辑有证据 | BACKLOG：新增 015（工具瞬时失败重试 P2，落点 guard 需授权）、016（会话级 token/成本预算断路器 P2）、017（弱点评测协议修订，**本轮落地 done**）；013 补轻量核验结果；无超 60 天 idea 条目 | **017 落地**：SKILL.md 研究节新增「弱点评测优先」步骤（选题从 RUNLOG/待人工决策/门禁失败挖掘弱点，轮换作兜底）；STATE.md 护栏新增第 6 条「评估器外置」原则；015/016 立项前只读核验完成（llm-retry=模型请求层重试、maxTokens=单请求输出上限，均非缺口所指层级）；013 轻量核验：12 技能均有 description，唯一捆绑脚本 encode_gif.py 无网络导入 | 无门禁（无代码改动）；自愈检查：无已合并进化改动、无回滚指示→无需回滚；上游漂移：**本轮 fetch 因 github.com:443 连接失败未实测**，沿用上轮实测 743（origin ref 未更新，仍 b150a551b8；本地 3d31e60e9e） | commit 待发布（见下）；`gh pr list` 因网络挂起，PR 状态本轮未确认 | ①人工优先收尾 web bind-address（漂移 743 持续四轮）；②工作区干净后完整模式先落地 001（hygiene 基线）；③下轮研究建议 #5 安全加固 + #2 MCP 生态（009/010 落地前补 MCP 信任边界审计）；④15/016 待人工授权（guard）；⑤若本轮推送 fork 失败，下轮重推 `git push --no-verify fork master:evolve/state` |
| 2026-08-24 | 安全加固（#5）+ MCP 生态（#2）（009/010 落地前补 MCP 信任边界审计，弱点评测优先） | ①OWASP MCP Top 10（2026）：MCP06 Intent Flow Subversion——工具描述被当作可信指令读取，防御=context isolation/instruction quarantine；②Christian Schneider「Securing MCP: defense-first」：tool description 即注入向量，客户端应在描述实质性变更时 re-prompt 用户或至少记录变更供安全审查；③imti.co「MCP Security and the Trust Boundary」：server 的 description/output/annotations 应 allow-listed/screened/consented/flagged；④OWASP AI Agent Security Cheat Sheet + Frontier Model Forum：harness 层是自然控制点，注入可存于 memory 跨 agent 传播，单一检测不足需分层；⑤SoK: The Attack Surface of Agentic AI：分层防御（content sanitization/provenance/ACL-aware retrieval/typed schemas/sandboxing/least-privilege/monitoring/kill-switches） | BACKLOG：**009/010 只读审计深化（双证据链确认缺口）**——①本地失衡证据：`packages/mcp/mcp-client/src/tools.ts` 对返回值侧防御严谨（extractText/decodeImage/projectContent 多处标注 network trust boundary），唯独 `tool.description`（L166）经 createDefinition 原样透传为模型可见描述（L257-258），无信任标记；②外部权威确认（OWASP MCP Top 10 / Christian Schneider / imti.co）；③010 缺口确认：connection.ts 重连/notification re-sync 会静默重写工具定义，swap 阶段无指纹对比；落点明确且有 reconnect.spec.ts 测试基础；④无超 60 天 idea 条目需清理（当前全为 evaluated/done） | **无代码改动**（研究模式：工作区 37 项在途，web bind-address 功能在途跨五轮，含 `packages/host/webserver/*`、`packages/bundle/web-app/*`、`docs/*`、`apps/cli/*` 及 `2026-08-20-explicit-web-interface-bind` note 3 文件） | 无门禁（无代码改动）；自愈检查：无已合并进化改动、无回滚指示→无需回滚；上游漂移：**补测实测 743**（本轮 fetch 因 github.com:443 超时未实测，但推送成功后网络恢复、补测 `git fetch origin` 成功——origin/master 仍 b150a551b8/dsh-v0.1.1-rc.2，2026-08-21 20:03 起上游无新提交，与沿用值一致），研究模式不强行同步 | commit `7b3c955131`（本地 master）→ fork `evolve/state`（`https://github.com/nightq/deepseek-harness/tree/evolve/state`），**推送成功**（`3d31e60e9e..7b3c955131`，含第四轮遗留 96dae81cf6，一次快进）；**PR 创建仍被拒**（GraphQL CreatePullRequest 权限不足，nightq 对上游仅 pull:true）→ 不重试，待人工建 PR | ①人工优先收尾 web bind-address 在途功能（漂移 743 连续五轮，过期基线成本递增）；②工作区干净后完整模式先落地 001（hygiene 基线）；③009 落地信任标记 / 010 落地定义指纹监控均需人工评审（010 若 re-prompt 用户触及 interaction 受保护路径）；④下轮研究建议 #6 性能 + #4 技能自我进化（或按弱点：若 009/010 进入落地评审，先补 MCP 描述变更的最小化方案评估）；⑤推送已成功，网络恢复，下轮 fetch 可实测漂移 |
| 2026-08-24 | 性能（#6）+ 技能自我进化（#4）（弱点评测优先：漂移归零核实 + 循环预算缺口挖掘） | ①**漂移归零**：人工于 2026-08-24 10:59 merge origin/master 进本地 master（commit 89ed947a41）→ HEAD..origin/master=0、origin/master..HEAD=8（7 个 chore(evolve) + 1 个 merge），落后 743 归零；origin 仍 b150a551b8/dsh-v0.1.1-rc.2 无新提交；merge 未触碰进化路径（`git diff 3bfd193460..89ed947a41 -- .agents/evolution .agents/skills/dsh-self-evolve` 为空）；②**新缺口 018（只读核实）**：`agent-loop/src/agent.ts` L199/L212/L263/L339 的 while 循环仅由模型产出终止，唯一上限 `maxTokens`（单步输出，sticky），`constants.ts` 仅 `DEFAULT_MAX_PARALLEL_TOOL_CALLS=10`（单步并行，已存在），`apps/cli` 无 turn 限制 → 会话级步骤预算真空缺；外部共识：arXiv 2603.05344「Iteration limits prevent runaway agent loops」「随会话增长资源必须有上限」（nudge budgets 已由 repeat-tool-reminder 阈值 [3,5,8] 天然覆盖）、dev.to「budgets(step/time/token/cost) 耗尽即优雅停止 + cache-aware 上下文排序（稳定前缀在前→印证 004）」、Medium 生产案例「无硬预算 → loop 200+ 步」；③技能自我进化：arXiv 2607.07663 SkillAxe（skill library + verification bottleneck + memory 累积）映射到既有 013/014；④StackOne Defender（22MB 提示注入检测模型）**评估拒绝（019）**：检测型防御可被绕过（第一轮结论）、新依赖违反护栏、与 003/006/009 架构级条目重叠；⑤在途工作扩大：除 web bind-address 外新增 **ai-app-platform 在途功能**（packages/ai-apps/、packages/client/ui-aiapps/、apps/cli/src/app.ts、docs/ai-app-platform-development-prompt.zh.md、2026-08-24-ai-app-platform note 6 文件）+ vendor/loader/src/config/*.{js,d.ts,map} 未跟踪构建产物 + wechat-article-scrape/ 未跟踪目录 | BACKLOG：**001 evaluated→accepted**（首落候选）；**新增 018**（agent-loop 会话级步骤预算，evaluated P2，落地形态待人工决策：改 core/agent-loop 非受保护但需架构文档+SDK 表面，或 guard 插件受保护）；**新增 019**（StackOne Defender，rejected）；016 补外部印证；无超 60 天 idea 条目 | **无代码改动**（研究模式：工作区 109 项在途 = 64 修改 + 45 未跟踪，web bind-address + ai-app-platform 双功能在途，含 `packages/host/webserver/*`、`packages/bundle/web-app/*`、`docs/*`、`apps/cli/*`、`packages/ai-apps/*`、`packages/client/ui-aiapps/*`、`vendor/loader/src/config/*.js` 未跟踪产物及 `2026-08-20-explicit-web-interface-bind`/`2026-08-24-ai-app-platform` note 文件） | 无门禁（无代码改动）；自愈检查：无已合并进化改动、无回滚指示→无需回滚；上游漂移：**实测 0**（人工 merge origin/master 已同步，见发现①），研究模式不强行 rebase | commit `acece7da78`（本地 master）→ fork `evolve/state`（`https://github.com/nightq/deepseek-harness/tree/evolve/state`），**推送成功**（`3bfd193460..acece7da78`，快进）；**PR 创建被拒**（GraphQL CreatePullRequest 权限不足）→ 不重试，待人工建 PR | ①人工优先收尾 web bind-address + ai-app-platform 双在途功能（merge 后未提交改动叠加在新基线上，收尾注意冲突）；②工作区干净后完整模式落地 001（hygiene 基线，首落）；③018 落地形态需人工决策（agent-loop 改动 vs guard 插件）；④009/010 落地评审、PR 创建均待人工；⑤下轮研究建议 #2 MCP 生态 + #1 agent 循环（或 009/010 进入落地评审时先补 MCP 描述变更最小化方案） |
| 2026-08-26 | MCP 生态（#2）+ agent 循环（#1）（弱点评测优先：009/010 落地评审前的「描述变更最小化方案」补充评估；本地复验行级证据） | ①官方 MCP Client Best Practices（2026-07-28 spec）：naive host 把每个 server 的工具定义原样塞给模型是反模式，官方最佳实践=客户端侧策展（工具定义=不可信输入）；②官方 Tool Annotations blog（2026-03-16）：spec 妥协=一切标注为「hint」，客户端 MUST 默认按不可信对待、按对 server 的信任度加权，「实际安全保证放确定性控制」→ **009/010 落地形态的 spec 原生锚点**：远端 description 按不可信 hint 对待（来源标注/指令隔离是客户端决定），010 最小安全子集（指纹+变更日志）属确定性控制，re-prompt 仅可选 UX；③DigitalApplied「19 个 MCP server 审计 taxonomy」：tool `description`=「server 编写、必填、直接展示给模型」（2026-07-28 rev）→ 009 前提获最新外部数据再确认；④Elastic Security Labs「MCP Tools: Attack Vectors and Defense」：tool poisoning/implicit tool call（隐藏指令劫持可信工具）；⑤Oracle「Agent Loop Decoded」：显式停止条件清单（iteration cap 默认 10、repetition detection 同 tool+args 连续 3 次、wall-clock timeout、unrecoverable errors）→ 018 外部权威再+1；⑥pipelab MCP Security：tool description 是首要威胁面；⑦**本地复验（新 checkout b150a551b8）**：009 证据行未变——`tools.ts` L165-166 `tool.description ?? ''` 原样传入 `createDefinition`（L251）→ L258 进入模型可见 `ToolDefinition.description`，无信任标记/来源标注；010——`connection.ts` L156-166 `enqueueSync` 串行化所有 syncTools（初始+notification re-sync）、L304 重连成功即 re-sync，swap 无指纹对比（grep fingerprint/hash 无命中） | BACKLOG：009/010 追加第七轮复验+外部新权威（落地形态细化：描述=不可信 hint、确定性控制优先，见落地记录）；018 追加 Oracle 权威（iteration cap 默认 10 参考值）；**新增 020**（harness 会话产物目录 `.dsh-pilot/`、`.dsh-uploads/` 入 `.gitignore`，evaluated P2，属研究模式误判源清理）；无超 60 天 idea 条目（当前全 evaluated/done/rejected） | **无代码改动**（研究模式：工作区 6 项未跟踪 = 3 项 web bind-address Agent Note（`2026-08-20-explicit-web-interface-bind.{md,zh.md,i18n.yaml}`）+ `docs/ai-app-platform-development-prompt.zh.md` + harness 会话产物 `.dsh-pilot/`（截图/下载）、`.dsh-uploads/`（上传文件）；**在途从 109 项缩至 6 项**——web bind-address 与 ai-app-platform 代码已提交 master，仅剩 note/文档与会话产物待收尾） | 无门禁（无代码改动）；自愈检查：无已合并进化改动、无回滚指示→无需回滚；上游漂移：**实测 0/0**（HEAD..origin/master=0 且 origin/master..HEAD=0；本地 master=origin=b150a551b8/dsh-v0.1.1-rc.2，上游自 2026-08-21 无新提交；**注意：本地 master 已被重置/重克隆为 origin/master，无历史 chore(evolve) 提交，进化历史仅存 fork evolve/state 且领先本地 master**） | commit 见本轮发布（本地 master）→ fork `evolve/state`（`https://github.com/nightq/deepseek-harness/tree/evolve/state`）；fork 分支领先本地 → 推送非快进，按协议删分支重推（推送前 `gh pr list --head nightq:evolve/state` 确认无现存 PR ✓）；PR 创建预期仍被拒（权限未变，nightq 对上游仅 pull）→ 记录 fork 分支 URL，不重试 | ①人工收尾在途 6 项（3 note + 1 文档 + 2 会话产物目录可忽略或经 020 入 .gitignore）后工作区干净 → 完整模式先落地 001（hygiene 基线）；②009/010 落地形态已按官方 spec 细化（描述=不可信 hint + 确定性控制），可进入人工评审；③018 落地形态（agent-loop vs guard）仍待人工决策；④下轮研究建议 #3 Cordis 插件生态 + #4 技能自我进化（或按弱点：001 落地时审计 hygiene 门禁输出基线） |
| 2026-08-27 | Cordis 插件生态（#3）+ 技能自我进化（#4）（弱点评测优先：**001 hygiene 基线实测**——研究模式首次运行只读门禁采集基线） | ①**hygiene 基线实测（首次）**：`pnpm run hygiene` = 11 passed / 2 failed（运行前后 `git status --porcelain` 无新文件，未污染工作区；失败与在途文件无关，属真实仓库基线弱点）——a) constraints：`packages/client/schema-form`、`packages/client/web-react` 空壳残留目录（仅 `lib/`+`node_modules`，无 package.json；git log 显示 refactor(client) `cf5e686408` 合并后未清理）→ `pnpm run clean` 可清理；b) vendor rescope：`knip.json` 中 `packages/util/home` workspace 块已不存在（moved，rescope-vendor.ts L201-214 期望片段过时）+ `adding-a-vendored-package.zh.md` 实际链接 `../rescope.zh.md` 而检查期望 `../rescope.md`（partial，L348-355 期望过时）→ 需更新 rescope-vendor.ts 两处期望片段；②Cordis 生态：Floatboat 第三方文章确认本仓库 Cordis 选择与生态主流一致（Koishi 4 年 4000+ 社区插件生产验证，cordiverse/paper 论文），无新缺口（与第四轮一致）；③技能自我进化：Claude Platform「Skill authoring best practices」description 必须同时含「做什么+何时用」；自改进技能三要素=二进制 pass/fail evals（主观评分不可靠）+ memory 教训累积 + wrap-up 步骤分离（Peter Yang / mindstudio / aiskill.market 共识）→ 与 013/014 及 STATE 护栏 6（评估器外置）覆盖一致，无需新条目；④**在途 6→7 项**：新增 `qwen-x-profile.md`（pilot_snapshot 的 X 主页快照导出=工具会话产物，非用户代码） | BACKLOG：**001 追加基线实测+修复清单**（constraints→`pnpm run clean`；vendor rescope→更新 rescope-vendor.ts 两处期望片段；均需完整模式执行）；013 追加评估维度补强（what+when 描述要求、二进制 eval 概念已由 014/护栏 6 覆盖）；无新条目；无超 60 天 idea 条目（当前全 evaluated/done/rejected） | **无代码改动**（研究模式：工作区 7 项未跟踪 = 3 项 web bind-address Agent Note + `docs/ai-app-platform-development-prompt.zh.md` + `.dsh-pilot/` + `.dsh-uploads/` + 新增 `qwen-x-profile.md`）；**SKILL.md 进程级修订**：研究节新增「研究模式可运行只读门禁/检查命令采集弱点基线（运行前后对比 git status 确认无新文件）」——固化本轮实践，属进化路径内允许变更 | **基线采集**（非改动门禁）：hygiene 11/13 绿、2 失败（详见发现①，001 落地时修复）；自愈检查：无已合并进化改动、无回滚指示→无需回滚；上游漂移：**实测 ahead=1 / behind=0**（HEAD=99d9158c72=第七轮 chore(evolve) 提交；origin/master 仍 b150a551b8/dsh-v0.1.1-rc.2，上游自 2026-08-21 无新提交）；研究模式不强行 rebase（ahead 全为进化提交，rebase 无意义） | commit 待发布（第八轮状态提交，含 SKILL.md 修订）→ fork `evolve/state`（`https://github.com/nightq/deepseek-harness/tree/evolve/state`）；**PR 无现存**（`gh pr list --head nightq:evolve/state` 为空，与第七轮一致） | ①人工收尾 7 项在途（含 `qwen-x-profile.md` 快照产物，非代码）；②工作区干净后完整模式落地 001——修复清单已就绪（`pnpm run clean` 清理残留 + 更新 rescope-vendor.ts 期望片段），落地后重跑 hygiene 验证基线归零；③009/010 落地评审待人工；④下轮研究建议 #5 安全加固 + #2 MCP 生态（或按弱点：001 落地验证后核对 hygiene 是否全绿） |
| 2026-08-28 | 依赖与代码卫生（#7）+ MCP 安全新权威（#5）（弱点评测优先：**上游 0.1.2-alpha.1 大版本对修复清单/审计结论的跨基线复核** + 009/010 评审支撑） | ①**上游漂移剧变（0→1079）**：`git fetch origin` 实测 behind=1079、ahead=4（全为 chore(evolve)，本地 tip=1f9de1865f 与 fork evolve/state 一致）；origin/master=cd5ef81481=**release/dsh-0.1.2-alpha.1**（上游 2026-08-21 后首次发布，含 refactor(repo): retire top-level examples、agent-presets 移至 packages/preset/ 等大规模重构）；**upstream 未触碰 `.agents/evolution/` 与 `.agents/skills/dsh-self-evolve/`**（`git log b150a551b8..origin/master -- <两路径>` 为空）→ 未来 rebase 预期无冲突；②**001 修复清单过时（部分已由上游修复）**：`git log b150a551b8..origin/master -- knip.json scripts/rescope-vendor.ts docs/cookbook/adding-a-vendored-package.zh.md` 三文件全变——上游已删除 `knip-logger-console` ExactEdit（util/home 块重构消失）、已将 `vendoring-cookbook-name-invariant-zh` 期望更新为 `../rescope.zh.md` → 第八轮「更新 rescope-vendor.ts 两处期望片段」**已由上游完成，001 修复清单收窄为仅 `pnpm run clean`**（本地磁盘 schema-form/web-react 残留确认仍在）；③**审计结论跨版本复核**：009 tools.ts 上游仅 1 提交（rename code-mode to ptc），L166 `tool.description ?? ''` 证据行未漂移；010 connection.ts 上游 0 提交（缺口原样）；018 agent.ts 上游 2 提交，`while (true)` L219/L270/L346 仍在且 grep 步骤上限无命中 → 009/010/018 缺口在 0.1.2-alpha.1 全部成立；④**009/010 新权威**：Speakeasy「Tool poisoning attacks against MCP servers」（2026）——「There is no boundary in the protocol that marks a tool description as data rather than instructions」=结构性根因（协议无数据/指令边界 → 客户端侧策展=009 落地形态直接支持）；「Version pinning：tools are pinned to a reviewed version by hash, so a rug pull changes the fingerprint and is rejected instead of silently trusted」→ **哈希指纹=业界标准 rug-pull 确定性控制=010 最小安全子集双权威确认**；「Model-layer defenses miss it; the MCP gateway is where it stops」（harness 层拦截点）；OWASP MCP Top 10 官方 MCP06 防御=Context isolation / instruction quarantine；⑤knip 实践（Effective TypeScript / dev.to）：unlisted deps 直报、rules 分级（exports/files/types=error）——与 001/002 现有覆盖一致，无新工具缺口 | BACKLOG：**001 追加跨基线复核**（修复清单收窄：仅 `pnpm run clean` + rebase 后重跑 hygiene 验证）；**009/010 追加第九轮复验+新权威**（结构性根因/version pinning/网关层拦截）；**018 追加跨基线复验**（缺口在 0.1.2-alpha.1 仍成立）；无新条目；无超 60 天 idea 条目（当前全 evaluated/done/rejected） | **无代码改动**（研究模式：工作区 29 项在途 = 16 修改 + 13 未跟踪——web bind-address 相关 3 项 Agent Note + `packages/session/session-persistence-jsonl/*`、`packages/client/*`、`docs/*`、`qwen-x-profile.md` 等；本轮核心产出=跨基线核验，无需门禁） | 自愈检查：无已合并进化改动、无回滚指示→无需回滚（落地数仍 0）；上游漂移：**实测 ahead=4 / behind=1079**（origin 发布 0.1.2-alpha.1；研究模式不强行 rebase，ahead 全为进化提交） | commit `0b38ddfabb`（本地 master）→ fork `evolve/state`（`https://github.com/nightq/deepseek-harness/tree/evolve/state`），**推送成功**（`1f9de1865f..0b38ddfabb`，快进，fork tip=0b38ddfabb）；`gh pr list --head nightq:evolve/state` 无现存 PR；`gh pr create` 实测被拒（GraphQL「nightq 无权 CreatePullRequest」，与第八轮一致）→ 不重试，待人工建 PR | ①人工收尾在途（含 session-persistence-jsonl 与 client 修改、3 项 Note、`qwen-x-profile.md` 快照产物）；②工作区干净后完整模式：**先 rebase origin/master（预期无冲突）再落地 001——修复清单已收窄为仅 `pnpm run clean` + hygiene 验证**；③009/010/018 审计结论在 0.1.2-alpha.1 全部成立，落地评审待人工；④下轮研究建议 #1 agent 循环 + #6 性能（或按弱点：001 落地后核对 hygiene 是否全绿） |

## 2026-08-27 并行实例核对（收敛补充）

第八轮由每日 08:05 定时实例（commit `12d9b57eb0`，已推送 fork `evolve/state`）与本次会话**并行各跑一轮**。双方独立复现同一结论——hygiene 基线 11/13、同样的 2 个失败与修复配方、在途 6→7（`qwen-x-profile.md` 归为工具会话产物）、漂移 ahead=1/behind=0——交叉验证通过，基线结论高置信。本次会话仅作补充性收敛：①**CI 覆盖归因**：`rescope-vendor:check` 不在 CI（仅本地 hygiene/check-all），constraints 失败为本地机器残留（CI 全新建树不受影响）→ 上游 CI 不因此红，但 codemod 与树失同步真实存在（下次 vendor 同步会咬）；②并发运行事实与处理教训（见 MEMORY 2026-08-27「并发实例检测」）。排程建议：避免同日多实例并行（last-writer-wins 会踩状态文件）。

## 2026-08-28 并行实例核对（收敛补充）

第九轮由每日 08:05 定时实例（commit `0b38ddfabb` 状态 + `60509542b2` 发布补录，已推送 fork `evolve/state`，tip=60509542b2，PR 创建实测被拒）与本次会话**并行各跑一轮（连续第二轮——排程修复未落地）**。双方独立结论一致——漂移 behind=1079（0.1.2-alpha.1）、001 修复清单收窄为仅 clean（rescope 期望由上游修复）、009/010/018 缺口跨版本成立、Speakeasy/OWASP MCP06 新权威。本次会话仅作补充性收敛（对方未覆盖的事实）：①**hygiene 复测实证**：`CI=true pnpm run hygiene` 实测仍 11/13 绿、同 2 失败（vendor rescope + constraints），运行前后 `git status --porcelain` 均 29 项、无新文件未污染 → 本地残留确认在（对方为推断，我方为实测），rescope 失败根因为本地脚本期望过期（上游已修）；②**在途清单实测 15M + 14?? = 29**（对方记 16+13，以实测为准），补充两类信号——`packages/session/session-persistence-jsonl/*` + `coordinator.ts` 触碰**受保护路径** `session/`（jsonl stale-append recovery 在途修复）；已归档 note `2026-07-20-jsonl-storage-identity.*` 被修改（归档冻结政策下异常，提请人工确认）；新增工具产物 `.workbuddy/`、`_tmp_*`×3（020 范围扩展候选）；③**009 基线事实补充**：server-qualified 公共工具名（`publicToolName`/`mcp__<serverName>__<rawName>`）在本地基线 b150a551b8 已存在（7 处命中）→ 来源标注由工具名承担是基线既有事实，009 缺口跨版本成立结论不变；④**并发碰撞连续两轮复现**：排程「当日是否已运行」检查未落地，第八、九轮连续双实例踩踏状态文件；具体机制建议（`git ls-remote fork evolve/state` tip 日期==今天则退出 / 单实例排程）已升级写入 STATE 待人工决策。本轮收敛补充提交与推送见 git log（`60509542b2..<收敛提交>` 快进 fork `evolve/state`）。

## 2026-08-28 手动会话并行核对（第二轮收敛补充）

本手动会话与上述定时实例并行（连续第二轮，排程机制未落地）。定时实例已完成三轮提交（`0b38ddfabb` 状态 / `60509542b2` 发布补录 / `a6f16ddb93` 收敛补充）；本会话独立研究（#5 安全加固 + #2 MCP 生态，弱点评测优先）经 grep 查重后仅写入对方未覆盖的**净新事实**：①**010 P2→P1 升级**——**CVE-2025-54136（MCPoison，NVD 有档）**：已信任 MCP 配置被静默替换为恶意命令、不触发任何警告/re-prompt（NVD + Check Point Cursor 实证），与 010 威胁模型完全同构；AgentMelt 2026（`list_changed` 仅客户端侧执行且不一致、无签名机制→客户端指纹是唯一强制机制）；CISA 2026-06-02 MCP 安全设计指南 → 真实 CVE + 落地形态就绪 + 非受保护路径 = P1 依据；②**009 实证权威**：CSA Labs 2026-07-01（353 工具×45 server×20 LLM 对抗描述变体实测）、CISA 2026-06-02、Invariant Labs 通知（「MCP 对工具描述过度信任」）；③**018 新权威**：Oracle 博客真实事故（坏工具 5 分钟 400 次调用、3 次迭代上限可阻止）+ Augment Code 成本测算（naive 10 步循环=43.3x）；④**对定时实例关键声明的独立复验（git show 只读，不污染工作区）**：漂移 behind=1079 ✓、009 L166 透传 ✓、010 enqueueSync/re-sync 无指纹 ✓、018 while(true) ✓、001 rescope 期望已由上游修复 ✓（`origin/master:scripts/rescope-vendor.ts` L198-199 `knip-bundle-base` 存在、zh 链接期望 `../rescope.zh.md`）、server-qualified 工具名基线既有 ✓（b150a551b8 `tools.ts` L111-112 `publicToolName`→`mcp__<serverName>__<rawName>`、L155/L161 使用）——双方独立结论全部一致，交叉验证高置信；⑤fork tip 观察：ls-remote 网络不稳，末次实测 `60509542b2`（`a6f16ddb93` 推送状态未确认），本补充提交后重推。

## 待人工决策

- **安全加固条目触碰受保护路径**：BACKLOG 003/004/005（提示注入边界、系统提示缓存友好、凭据脱敏）改动面落在 `session/`、`guard/`、`credentials/` 等受保护路径，按护栏需人工授权改动范围后才能落地；012（错误分类 nudge）若落点 guard 同样需授权；**015（工具瞬时失败重试）/016（会话级 token/成本预算断路器）第四轮已核验缺口真实，落点涉 `guard/`，落地同样需授权**。
- **上游漂移**：~~本地 master 落后 origin/master 743 提交~~ **已解决**——人工于 2026-08-24 10:59 将 origin/master merge 进本地 master（commit `89ed947a41`），实测落后归零（HEAD..origin/master=0）；origin 自 dsh-v0.1.1-rc.2（b150a551b8）无新提交。**第七轮新情况**：本地 master 已被重置/重克隆为 origin/master（`b150a551b8`，与 origin 完全一致，0 前 0 后），历史 chore(evolve) 提交与 merge 均不在本地——进化历史仅存于 fork `evolve/state`（领先本地 master），故本轮推送为非快进，按协议删分支重推（内容经状态文件完整保留）。
- **018 落地形态（需人工决策）**：agent-loop 会话级步骤/迭代预算缺口已只读核实（第六轮，见上表发现②）。两种落地形态：A）改 `packages/core/agent-loop`（非受保护路径，但需同步 docs/architecture.md + SDK 表面 + testing policy 快照）；B）guard 插件（事件计数熔断，受保护路径需授权）。与 016（token/成本预算）同族，建议一起出设计。
- **PR 创建失败（需人工建 PR）**：状态已推送至 fork `evolve/state`（`https://github.com/nightq/deepseek-harness/tree/evolve/state`），但 `gh pr create --repo deepseek-ai/deepseek-harness` 被拒：GraphQL 报「nightq 无权 CreatePullRequest」，REST pulls 端点 404；token 对上游权限仅 `pull:true`。第八轮：`gh pr list --head nightq:evolve/state` 确认**仍无现存 PR**；推送成功两次（99d9158c72..12d9b57eb0、12d9b57eb0..54d902e23a，均快进，fork tip=54d902e23a）；`gh pr create` 实测被拒（2026-08-27 08:06 GraphQL「nightq 无权 CreatePullRequest」）→ 不重试，fork 分支 URL 已记录 → 请人工创建 PR（head `nightq:evolve/state` → base `master`，title 建议 `chore(evolve): 每日进化状态 2026-08-27`），或为本账号开通对上游的 PR 创建权限后下轮自动重试。
- ~~**第四轮发布网络异常**~~：已解决——第五轮推送成功（`3d31e60e9e..7b3c955131`），网络恢复。
- ~~**发布路径尚未打通**~~：已解决——fork `nightq/deepseek-harness` 存在，2026-08-21 第二轮打通 `evolve/state` 分支 + PR。
